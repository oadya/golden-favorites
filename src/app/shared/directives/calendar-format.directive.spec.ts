import { CalendarFormatDirective } from './calendar-format.directive';

describe('CalendarFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarFormatDirective();
    expect(directive).toBeTruthy();
  });
});
